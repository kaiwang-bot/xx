```mermaid
graph TD
    subgraph A[🚀 用户界面层 (HarmonyOS 原生)]
        A1[ArkUI 界面 (聊天/联系人/设置)] --> A2[平台服务调用 (通知/文件)]
    end

    subgraph B[🧠 业务逻辑层 (HarmonyOS 桥接)]
        B1[TDLib 绑定层 (JNI/FFI)] --> B2[状态管理 / 数据转换]
        B2 --> A1
        TDLib_Out --> B1
        B1 --> TDLib_In
    end

    subgraph C[⚙️ 数据与网络核心层 (TDLib)]
        C1[TDLib 核心] -- 事件通知 (TDLib_Out) --> B
        C1 -- 命令调用 (TDLib_In) --> B
        C1 --> C2[MTProto 网络协议]
        C1 --> C3[本地数据库/缓存]
    end

    A -- 用户操作 --> B
    B -- 业务逻辑 --> C
    C -- 数据更新 --> B
```

